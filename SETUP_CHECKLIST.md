# Чеклист налаштування після міграції

## ✅ Що вже зроблено

1. ✅ Скопійовано весь фронтенд (admin-panel)
   - 10 папок зі сторінками (properties, login, dashboard, settings, тощо)
   - Всі компоненти та налаштування

2. ✅ Скопійовано весь бекенд (admin-panel-backend)
   - 14 роутів (properties, auth, public, settings, тощо)
   - Всі entities, middleware, utils

3. ✅ Налаштовано next.config.js
   - API URL: `https://app.pro-part.online/api` (для production)
   - Fallback на `http://localhost:4000/api` (для локальної розробки)
   - Автоматичне визначення середовища (development/production)

4. ✅ Створено docker-compose.yml для локальної бази даних

---

## 📋 Наступні кроки

### 1. Перевірити та налаштувати .env файл в admin-panel-backend

Перевірте наявність та коректність наступних змінних в `.env` файлі:

```env
# База даних
DATABASE_URL=postgresql://admin:admin123@localhost:5436/admin_panel_propart

# Порт бекенду
PORT=4000

# JWT секрет для авторизації
ADMIN_JWT_SECRET=your-secret-key-here

# Email налаштування (якщо використовується)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=your-password

# Cloudinary (якщо використовується для завантаження зображень)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
```

**Якщо .env файл відсутній:**
```bash
cd admin-panel-backend
cp .env.example .env
# Відредагуйте .env файл з вашими налаштуваннями
```

### 2. Налаштувати базу даних

#### Варіант A: Docker (рекомендовано для локальної розробки)

```bash
cd admin-panel-backend
docker-compose up -d admin-panel-db
```

Перевірте, що база даних запущена:
```bash
docker ps | grep admin-panel-db
```

#### Варіант B: Зовнішня база даних

Якщо ви використовуєте зовнішню базу даних:
1. Переконайтеся, що `DATABASE_URL` в `.env` вказує на правильну БД
2. Перевірте підключення до БД
3. Запустіть міграції (якщо потрібно):
   ```bash
   cd admin-panel-backend
   npm run migration:run
   ```

### 3. Встановити залежності

#### Бекенд:
```bash
cd admin-panel-backend
npm install
```

#### Фронтенд:
```bash
cd admin-panel
npm install
```

### 4. Запустити бекенд

```bash
cd admin-panel-backend
npm run dev
```

Перевірте:
- ✅ Підключення до БД (має з'явитися повідомлення "✅ Database connected")
- ✅ Відсутність помилок при старті
- ✅ Сервер слухає на `http://localhost:4000`
- ✅ Health check доступний: `http://localhost:4000/health`

### 5. Запустити фронтенд

```bash
cd admin-panel
npm run dev
```

Перевірте:
- ✅ Фронтенд запускається на `http://localhost:3001`
- ✅ Логін працює
- ✅ Properties завантажуються
- ✅ API запити відправляються на правильний URL

### 6. Перевірити дані в базі

Якщо база порожня, можливо знадобиться:

#### Імпорт даних:
```bash
cd admin-panel-backend
npm run seed
```

#### Створення тестових користувачів:
Перевірте скрипт `src/seeds/seed.ts` для створення тестових даних.

---

## 🔍 Перевірка налаштувань

### Перевірка API URL

Фронтенд автоматично визначає API URL:
- **Development**: `http://localhost:4000/api`
- **Production**: `https://app.pro-part.online/api`

Можна перевизначити через змінну оточення:
```bash
# В admin-panel/.env.local
NEXT_PUBLIC_API_URL=http://localhost:4000/api
```

### Перевірка підключення до БД

```bash
# Перевірка через health check
curl http://localhost:4000/health

# Очікувана відповідь:
# {
#   "status": "ok",
#   "database": "connected",
#   "timestamp": "2024-01-01T00:00:00.000Z"
# }
```

### Перевірка авторизації

1. Відкрийте `http://localhost:3001/login`
2. Спробуйте увійти з тестовими обліковими даними
3. Перевірте, що токен зберігається в localStorage
4. Перевірте, що API запити містять заголовок `Authorization: Bearer <token>`

---

## 🐛 Вирішення проблем

### Проблема: БД не підключається

**Рішення:**
1. Перевірте, що Docker контейнер запущений:
   ```bash
   docker ps | grep admin-panel-db
   ```
2. Перевірте `DATABASE_URL` в `.env` файлі
3. Перевірте логи:
   ```bash
   docker logs admin-panel-db
   ```

### Проблема: API запити не проходять

**Рішення:**
1. Перевірте, що бекенд запущений на порту 4000
2. Перевірте `NEXT_PUBLIC_API_URL` в `.env.local`
3. Перевірте CORS налаштування в бекенді

### Проблема: Помилки авторизації

**Рішення:**
1. Перевірте `ADMIN_JWT_SECRET` в `.env` файлі бекенду
2. Перевірте, що токен правильно передається в заголовках
3. Перевірте middleware авторизації

---

## 📝 Додаткові налаштування

### Cloudinary (для завантаження зображень)

Якщо використовується Cloudinary для завантаження зображень:
1. Створіть акаунт на [Cloudinary](https://cloudinary.com)
2. Додайте налаштування в `.env` файл бекенду
3. Перевірте файл `CLOUDINARY_SETUP.md` для деталей

### Email налаштування

Якщо використовується відправка email:
1. Налаштуйте SMTP сервер в `.env` файлі
2. Перевірте файл `src/services/email.service.ts`

---

## 🚀 Готово!

Після виконання всіх кроків проект повинен працювати:

- ✅ Бекенд доступний на `http://localhost:4000`
- ✅ Фронтенд доступний на `http://localhost:3001`
- ✅ База даних підключена
- ✅ Авторизація працює
- ✅ API запити проходять успішно

Якщо виникли проблеми, перевірте розділ "Вирішення проблем" вище.

