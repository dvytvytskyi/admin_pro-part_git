# 🔐 Налаштування .env файлів для деплою

## 📋 Які .env файли потрібні

Для продакшн сервера потрібно створити **2 файли**:

1. **`/admin-panel-backend/.env`** - для Backend
2. **`/admin-panel/.env.production`** - для Frontend (опціонально, якщо використовується)

---

## 🔧 Backend .env файл

**Шлях:** `/admin-panel-backend/.env`

```env
# ============================================
# DATABASE
# ============================================
# Для продакшн - замінити на реальний URL БД на сервері
# Формат: postgresql://user:password@host:port/database
DATABASE_URL=postgresql://admin:YOUR_DB_PASSWORD@admin-panel-db:5432/admin_panel

# Або якщо БД на окремому сервері:
# DATABASE_URL=postgresql://admin:password@your-db-host:5432/admin_panel

# ============================================
# JWT & AUTHENTICATION
# ============================================
# ⚠️ ВАЖЛИВО: Змінити на унікальний секрет для продакшн!
ADMIN_JWT_SECRET=your_super_secret_jwt_key_change_this_in_production_min_32_chars

# ============================================
# ADMIN LOGIN
# ============================================
# ⚠️ ВАЖЛИВО: Змінити на свої дані для входу в адмінку!
ADMIN_EMAIL=admin@pro-part.online
ADMIN_PASSWORD=your_secure_admin_password_here

# ============================================
# CLOUDINARY (для зображень)
# ============================================
# Можна використовувати або CLOUDINARY_URL, або окремі змінні
CLOUDINARY_URL=cloudinary://api_key:api_secret@cloud_name

# Або окремі змінні:
CLOUDINARY_CLOUD_NAME=dgv0rxd60
CLOUDINARY_API_KEY=141613625537469
CLOUDINARY_API_SECRET=GgziMAcVfQvOGD44Yj0OlNqitPg

# ============================================
# SERVER
# ============================================
NODE_ENV=production
PORT=4000

# ============================================
# DATABASE PASSWORD (для docker-compose)
# ============================================
DB_PASSWORD=your_secure_db_password_here
```

---

## 🎨 Frontend .env файл (опціонально)

**Шлях:** `/admin-panel/.env.production`

```env
# ============================================
# API URL
# ============================================
# URL вашого Backend API на продакшн сервері
NEXT_PUBLIC_API_URL=https://admin.pro-part.online/api

# ============================================
# NextAuth
# ============================================
# URL фронтенду
NEXTAUTH_URL=https://admin.pro-part.online

# ⚠️ ВАЖЛИВО: Змінити на унікальний секрет!
NEXTAUTH_SECRET=your_nextauth_secret_here_min_32_chars

# ============================================
# ENVIRONMENT
# ============================================
NODE_ENV=production
```

---

## ⚠️ ЩО ОБОВ'ЯЗКОВО ЗМІНИТИ ПЕРЕД ДЕПЛОЄМ

### 🔴 КРИТИЧНО (безпека):

1. **`ADMIN_JWT_SECRET`** - згенерувати унікальний секрет (мінімум 32 символи)
   ```bash
   # Згенерувати секрет:
   openssl rand -base64 32
   ```

2. **`ADMIN_EMAIL`** - змінити на свій email для входу в адмінку

3. **`ADMIN_PASSWORD`** - змінити на безпечний пароль для входу в адмінку
   ```bash
   # Згенерувати пароль:
   openssl rand -base64 16
   ```

4. **`NEXTAUTH_SECRET`** - згенерувати унікальний секрет (для frontend)
   ```bash
   openssl rand -base64 32
   ```

5. **`DB_PASSWORD`** - змінити на безпечний пароль для БД

6. **`DATABASE_URL`** - перевірити, що вказує на правильну БД на сервері

### 🟡 РЕКОМЕНДОВАНО:

1. **`CLOUDINARY_*`** - перевірити, що ключі правильні
2. **`NEXT_PUBLIC_API_URL`** - перевірити домен
3. **`NEXTAUTH_URL`** - перевірити домен

---

## 📝 Швидка інструкція для деплою

### 1. Створити .env файли на сервері:

```bash
# На сервері, в корені проекту:
cd /path/to/admin_pro-part

# Створити Backend .env
nano admin-panel-backend/.env
# (вставити вміст зверху, змінивши секрети)

# Створити Frontend .env (якщо потрібно)
nano admin-panel/.env.production
# (вставити вміст зверху, змінивши секрети)
```

### 2. Перевірити права доступу:

```bash
# .env файли мають бути доступні тільки для власника
chmod 600 admin-panel-backend/.env
chmod 600 admin-panel/.env.production
```

### 3. Перезапустити контейнери:

```bash
docker-compose -f docker-compose.prod.yml down
docker-compose -f docker-compose.prod.yml up -d
```

---

## 🔍 Перевірка після деплою

```bash
# Перевірити, що Backend запустився:
curl http://localhost:4000/api/health

# Перевірити логи:
docker logs for-you-admin-panel-backend-prod

# Перевірити, що .env завантажився:
docker exec for-you-admin-panel-backend-prod env | grep DATABASE_URL
```

---

## 💡 Примітки

- **НЕ комітьте .env файли в Git!** Вони мають бути в `.gitignore`
- Якщо використовуєте Docker Compose, він автоматично завантажить `.env` з кореня проекту
- Для продакшн краще використовувати секрет-менеджери (AWS Secrets Manager, HashiCorp Vault тощо)

