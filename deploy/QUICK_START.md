# Швидкий старт деплою

## Автоматичний деплой

Просто запустіть:

```bash
cd /Users/vytvytskyi/admin_pro-part
./deploy/deploy-pro-part.sh
```

Скрипт автоматично:
- ✅ Підключиться до сервера
- ✅ Встановить необхідні пакети
- ✅ Скопіює файли проекту
- ✅ Збудує Docker образи
- ✅ Запустить контейнери
- ✅ Налаштує Nginx
- ✅ Отримає SSL сертифікат

## Важливо перед деплоєм

Переконайтеся, що на сервері є `.env` файли:

1. `/opt/admin-pro-part/.env` - основний файл з налаштуваннями
2. `/opt/admin-pro-part/admin-panel-backend/.env` - налаштування backend

Якщо файлів немає, створіть їх на сервері вручну або скопіюйте з локальної машини:

```bash
# Підключіться до сервера
ssh root@88.99.38.25

# Створіть .env файли
cd /opt/admin-pro-part
nano .env
nano admin-panel-backend/.env
```

## Після деплою

Перевірте:
- 🌐 https://admin.pro-part.online - головна сторінка
- 🔗 https://admin.pro-part.online/api - API endpoint

## Проблеми?

Дивіться детальну інструкцію: [DEPLOY_PRO_PART.md](./DEPLOY_PRO_PART.md)

