# 🚀 Швидкий старт

## Крок 1: Налаштування бази даних

```bash
cd admin-panel-backend
docker-compose up -d admin-panel-db
```

Перевірте, що база даних запущена:
```bash
docker ps | grep admin-panel-db
```

## Крок 2: Налаштування .env файлів

### Бекенд (.env)
```bash
cd admin-panel-backend
# Якщо .env файл відсутній:
cp .env.example .env
# Відредагуйте .env файл з вашими налаштуваннями
```

Мінімальні налаштування:
```env
DATABASE_URL=postgresql://admin:admin123@localhost:5436/admin_panel_propart
PORT=4000
ADMIN_JWT_SECRET=your-secret-key-here
```

### Фронтенд (.env.local)
```bash
cd admin-panel
# Якщо .env.local файл відсутній:
cp .env.example .env.local
```

Для локальної розробки (опціонально):
```env
NEXT_PUBLIC_API_URL=http://localhost:4000/api
```

## Крок 3: Встановлення залежностей

### Бекенд:
```bash
cd admin-panel-backend
npm install
```

### Фронтенд:
```bash
cd admin-panel
npm install
```

## Крок 4: Запуск проекту

### Термінал 1 - Бекенд:
```bash
cd admin-panel-backend
npm run dev
```

Перевірте:
- ✅ Сервер запущений на `http://localhost:4000`
- ✅ База даних підключена (повідомлення "✅ Database connected")
- ✅ Health check доступний: `http://localhost:4000/health`

### Термінал 2 - Фронтенд:
```bash
cd admin-panel
npm run dev
```

Перевірте:
- ✅ Фронтенд запущений на `http://localhost:3001`
- ✅ Логін доступний: `http://localhost:3001/login`

## Крок 5: Перевірка

1. Відкрийте `http://localhost:3001/login`
2. Спробуйте увійти з тестовими обліковими даними
3. Перевірте, що Properties завантажуються

## 🔍 Перевірка API

```bash
# Health check
curl http://localhost:4000/health

# Очікувана відповідь:
# {
#   "status": "ok",
#   "database": "connected",
#   "timestamp": "2024-01-01T00:00:00.000Z"
# }
```

## 🐛 Вирішення проблем

### БД не підключається
```bash
# Перевірте, що Docker контейнер запущений
docker ps | grep admin-panel-db

# Перевірте логи
docker logs admin-panel-db

# Перезапустіть контейнер
docker-compose restart admin-panel-db
```

### API запити не проходять
1. Перевірте, що бекенд запущений на порту 4000
2. Перевірте `NEXT_PUBLIC_API_URL` в `.env.local`
3. Перевірте CORS налаштування в бекенді

### Помилки авторизації
1. Перевірте `ADMIN_JWT_SECRET` в `.env` файлі бекенду
2. Перевірте, що токен правильно передається в заголовках

## 📝 Додатково

Детальний чеклист налаштування: [SETUP_CHECKLIST.md](./SETUP_CHECKLIST.md)

